with cte as (
SELECT *, row_number() over (
partition by user_id order by transaction_date) as transaction_no
FROM transactions
)

select user_id, spend, transaction_date
from cte where transaction_no = 3
