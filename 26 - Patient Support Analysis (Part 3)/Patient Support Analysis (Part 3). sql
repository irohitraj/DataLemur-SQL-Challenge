with cte as (
SELECT policy_holder_id, call_date,
LEAD(call_date) over (PARTITION BY policy_holder_id order by call_date ) as second_call_date
FROM callers
)

select count(distinct policy_holder_id) as policy_holder_count
from cte 
where  date(second_call_date) - date(call_date) < 7
