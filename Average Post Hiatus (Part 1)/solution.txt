SELECT user_id,
EXTRACT (DAY FROM (MAX(post_date) -  MIN(post_date))) as days_between
from posts
where EXTRACT(YEAR FROM post_date) = 2021
group by user_id
having count(*)>=2


-- did not use FIRST_VALUE as it generates another column 
-- and one can use DATE_PART as well
